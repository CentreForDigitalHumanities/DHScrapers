apiVersion: batch/v1
kind: Job
metadata:
  name: harvest-job
spec:
  template:
    spec:
      containers:
      - name: ubuntu
        image: ubuntu
        args:
        - sleep
        - "2d"
      restartPolicy: OnFailure